If choose to run dockerfile because it is built based on NGC docker, so there are many dependencies already installed, only need to install some of the packages described in the dockerfile.

If choose to build docker from scratch, need to install packages in the requirements at least.

Base files are cluster training results for visulization(Tableu).
